import random
def num_random():

    numero_random= [random.randint(0,100) for i in range(30)]
    return numero_random

def ordenar_lista(lista):
    lista_ordenada = sorted(lista)
    return lista_ordenada
